{% extends 'admin/base.html.twig' %}

{% block body %}

    <div class="panel panel-default">

    <div class="panel-heading">
        <h2><i class="glyphicon glyphicon-list-alt"></i> {{ 'user.change_password.title'|trans }}</h2>
    </div>

    <div class="panel-body">

        {{ include('_partials/_alerts.html.twig') }}

        {{ form_start(form, { 'action': path('admin_change_password'), 'attr': {'novalidate': 'novalidate'} }) }}

        {{ form_errors(form) }}

        {{ form_row(form.current_password) }}
        {{ form_row(form.plainPassword.first) }}
        {{ form_row(form.plainPassword.second) }}

        <div class="row">
            <div class="col-md-12">
                <button class="btn btn-primary">{{ 'user.change_password.btns.submit'|trans }}</button>
            </div>
        </div>

        {{ form_widget(form._token) }}

        {{ form_end(form, {'render_rest': false}) }}

    </div>

{% endblock %}