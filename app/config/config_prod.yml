imports:
    - { resource: config.yml }

parameters:
    email_sender: "postmaster@app6b8d3e36f7864da3b5aac5ce497a6678.mailgun.org"
    email_contact_to: "neandher89@gmail.com"
    http_protocol: http

monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: nested
        nested:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: debug
        console:
            type: console
            process_psr_3_messages: false

lexik_maintenance:
    authorized:
        ips: ['177.137.232.50']